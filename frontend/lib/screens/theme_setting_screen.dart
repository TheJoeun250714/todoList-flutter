import 'package:flutter/material.dart';

class theme_setting_screen extends StatelessWidget {
  const theme_setting_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('theme_setting_screen is working'),
      ),
    );
  }
}